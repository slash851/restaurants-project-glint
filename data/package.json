{
  "name": "data",
  "version": "1.0.1",
  "description": "populating raw data to sequelized database",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "clean": "rm -fr dist",
    "copy_assets": "cp -r src/assets dist/assets",
    "start": "yarn run build && yarn run copy_assets && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sequelize/core": "^7.0.0-alpha.10",
    "dotenv": "^16.0.0",
    "fs": "0.0.1-security",
    "moment": "^2.29.1",
    "pg": "^8.7.3",
    "readline": "^1.3.0",
    "sequelize": "^6.17.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.25"
  }
}
